[package]
name = "sakuramml-core"
description = "MML (Music Macro Language) to Standard MIDI File compiler core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# No dependencies: the CP932 tables are generated into the crate itself
# (tools/gen_cp932.py), which keeps the WASM binary small.
