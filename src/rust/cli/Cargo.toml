[package]
name = "sakuramml-cli"
description = "Command line MML to MIDI compiler (csakura replacement)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "csakura"
path = "src/main.rs"

[dependencies]
sakuramml-core = { path = "../core" }
