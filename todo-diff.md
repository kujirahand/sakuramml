# sample MML の Pascal/Rust 差異 TODO

調査日: 2026-09-14
対象: `sample/*.mml`（`random.mml` は比較対象外）
基準: `src/pascal/csakura` が生成する MIDI と正規化イベント列

## 調査方法

`just test-samples` で両コンパイラをビルドし、各サンプルについて生成可否、MIDI バイト列、
Rust CLI の `--dump-midi` で正規化したイベント列を比較した。

`169.mml` は曲中で `v.Random(3)` を多数使うため、velocity の値そのものは修正対象にしない。
ただし、同ファイルで見つかった構文エラー、音長、ゲート、時刻の差は乱数と無関係なので対象に含める。

| sample | Pascal | Rust | byte差 | event差 | 判定 |
|---|---:|---:|---:|---:|---|
| `169.mml` | OK | OK | 16.38% | 64.69% | 乱数以外のゲート差あり |
| `230.mml` | OK | OK | 48.67% | 4.13% | 差あり |
| `4.mml` | OK | OK | 72.88% | 2.28% | 差あり |
| `40.mml` | OK | OK | 0.08% | 0.13% | 差あり |
| `hy_joe.mml` | OK | OK | 89.31% | 7.11% | 差あり |
| `test_01.mml` | OK | OK | 56.26% | 5.25% | 差あり |

`23.mml`、`47.mml`、`66.mml`、`sakura2.mml`、`seija.mml`、`sutoton-9.mml` は
MIDIバイト列と正規化イベント列が完全一致したため、以下には残課題だけを記載する。

## サンプル単位で残る音長・時刻・順序の差

### 1. 和音の有効音長と NoteOff を Pascal 互換にする

- 影響: `4.mml`、`40.mml`、`169.mml`
- 反復と先行指定を組み合わせた和音に、音長、ゲート、同時刻のNoteOff順の差が残っている。
- 和音を保留してまとめて出力する処理で、各音の有効音長と反復開始時の状態を保持し、
  Pascal生成MIDIを基準にゴールデンテストへ落とす。

### 2. 反復・連符・先行指定を組み合わせた状態遷移を直す

- 影響: `4.mml`、`40.mml`
- `4.mml` は `【7 連符{...}】`、反復終端、結合音、`v.onNote` の組み合わせで、音長、開始時刻、
  ゲート、Program Change の位置がずれる。
- `40.mml` は反復内の和音と `t.onNote` で、和音各音の timing 値、音長、ゲートがずれ、
  トラック5にはRustだけの余分な音符が1個出る。
- 反復の通常終端・代替終端で、音符属性の添字、保留和音、直前音、既定音長をどこまで復元するかを
  Pascal の処理順に合わせる。

### 3. CC先行指定の時刻・補間終端・削除範囲を揃える

- 影響: `230.mml`、`hy_joe.mml`、`test_01.mml`
- `EP.onTime`、`P.onTime`、RPN/NRPN 初期化では、開始時刻、`ControllerShift` 分の先行、
  補間終端、同時刻イベント順に差がある。
- `230.mml` では CC1 の開始・終了時刻が小節単位でずれ、`hy_joe.mml` と `test_01.mml` には
  補間終端と同時刻イベント順の差が残っている。
- `WriteCCWave` と `DeleteCCAfterTime` の規則を個別の最小ゴールデンテストへ落とす。

### 4. `MeasureShift` を含む負時刻と先行イベントを揃える

- 影響: `169.mml`、`hy_joe.mml`
- `System.MeasureShift(4)` と負方向の休符、`p%.onTime`、`y11.onTime` を組み合わせたとき、
  先行イベントの時刻と同時刻順に差が残る。
- 負時刻のイベントを途中で0へ丸めず、ソート後のSMF delta生成時だけ安全に処理するという
  Pascal互換の境界を、CC、ベンド、RPN/NRPNの全経路で統一する。

## 完了条件

1. 上記ごとに Pascal 生成 MIDI を基準にした最小ゴールデンテストを追加する。
2. 残る非乱数5ファイルで、正規化イベント列とMIDIバイト列を一致させる。
3. `169.mml` は `v.Random` の値を正規化した上で、音高、音長、ゲート、時刻、イベント数を一致させる。
4. `random.mml` と乱数系列そのものの一致は完了条件に含めない。
